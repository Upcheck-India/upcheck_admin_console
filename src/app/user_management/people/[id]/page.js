// People record profile page — reuses the same rich profile UI as /user_management/[id]
// but fetches from /api/people/[id] which returns the people_records document.
export { default } from '../../[id]/page';
